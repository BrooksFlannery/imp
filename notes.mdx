cmd shift l opens new chat tab in ask mode?
i need to follow up with cmd i to switch to agent mode
only after creation of new chat using cmd shift l
if we are doing concurrent phases(not first one though) we can just keep doing cmd t to open new tab as we are bc that does default to agent mode